{
  "name": "{{input `name`}}",
  "version": "0.2.0",
  "description": "{{input `description`}}",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -L -i test -i segments -i node_modules",
    "test": "mocha --recursive --require should"
  },
  "author": "{{input `author`}}",
  "license": "SEE LICENSE IN LICENSE",
  "dependencies": {
    "express": "^4.14.0",
    "@indigoframework/agent": "^0.2.0-3"
  },
  "devDependencies": {
    "mocha": "^2.5.3",
    "nodemon": "^1.9.2",
    "should": "^10.0.0"
  }
}
