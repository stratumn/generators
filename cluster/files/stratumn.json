{
  "name": "{{.dir}}",
  "author": "{{input `author`}}",
  "description": "Indigo Tendermint Node test cluster",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
{{- if eq (input `useGit`) `y` }}
    "init": "strat run tm:init && strat run tm:clusterize && git init && git add .",
    "init:linux": "strat run tm:init && strat run tm:post-init:linux && strat run tm:clusterize && git init && git add .",
{{- else }}
    "init": "strat run tm:init && strat run tm:clusterize",
    "init:linux": "strat run tm:init && strat run tm:post-init:linux && strat run tm:clusterize",
{{- end}}
    "tm:init": "./scripts/tm-init.sh",
    "tm:post-init:linux": "./scripts/tm-linux-post-init.sh && ./arch/linux/fix-monitoring-files-ownership.sh",
    "tm:clusterize": "./scripts/tm-clusterize.sh",
    "compose": "docker-compose -p {{input `author`}}-dev -f docker-compose.yml -f docker-compose.dev.yml",
    "build": "strat run compose build",
    "up": "strat run build && strat run compose up",
    "down": "strat run compose down",
    "pull": "strat run compose pull",
    "push": "strat run compose push"
  }
}
